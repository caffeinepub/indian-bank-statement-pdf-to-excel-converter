// This file is intentionally minimal as the app is fully client-side
// No backend queries are needed for the PDF to Excel conversion

export function useQueries() {
  // Placeholder for future backend integration if needed
  return {};
}
